# ASL Gesture System

## Setup

1. Create environment:
   python3.11 -m venv asl
   source asl/bin/activate

2. Install dependencies:
   pip install mediapipe opencv-python numpy scikit-learn pyttsx3

3. Download dataset (link provided separately)

4. Run:
   python inference.py